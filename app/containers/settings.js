import React, { Fragment } from 'react';
import Helmet from 'react-helmet';

export default () => (
  <Fragment>
    <Helmet
      title="Settings | React 16 Webpack 4"
    />
    <div>Settings</div>
  </Fragment>
);
